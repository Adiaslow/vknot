---
/** Assumption — thin wrapper over <Environment kind="assumption">. */
import Environment from "./Environment.astro";

export interface Props {
  title?: string;
  number?: string;
}

const { title, number } = Astro.props;
---

<Environment kind="assumption" title={title} number={number}>
  <slot />
</Environment>
