---
/** Conjecture — thin wrapper over <Environment kind="conjecture">. */
import Environment from "./Environment.astro";

export interface Props {
  title?: string;
  number?: string;
}

const { title, number } = Astro.props;
---

<Environment kind="conjecture" title={title} number={number}>
  <slot />
</Environment>
