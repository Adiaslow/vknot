---
/** Definition — thin wrapper over <Environment kind="definition">. */
import Environment from "./Environment.astro";

export interface Props {
  title?: string;
  number?: string;
}

const { title, number } = Astro.props;
---

<Environment kind="definition" title={title} number={number}>
  <slot />
</Environment>
