---
/** Example — thin wrapper over <Environment kind="example">. */
import Environment from "./Environment.astro";

export interface Props {
  title?: string;
  number?: string;
}

const { title, number } = Astro.props;
---

<Environment kind="example" title={title} number={number}>
  <slot />
</Environment>
