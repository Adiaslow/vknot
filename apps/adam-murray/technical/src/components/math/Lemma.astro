---
/** Lemma — thin wrapper over <Environment kind="lemma">. */
import Environment from "./Environment.astro";

export interface Props {
  title?: string;
  number?: string;
}

const { title, number } = Astro.props;
---

<Environment kind="lemma" title={title} number={number}>
  <slot />
</Environment>
