---
export interface Props {
  title?: string;
  number?: string;
}

const { title, number } = Astro.props;
---

<div class="my-6 rounded-lg border-l-4 border-emerald-600 bg-white p-5 dark:bg-slate-900 dark:border-emerald-400">
  <div class="flex items-baseline gap-2 mb-2">
    <strong class="font-semibold text-emerald-700 dark:text-emerald-300">
      Lemma
      {number && <span class="ml-1">{number}</span>}
      {title && <span class="ml-1">({title})</span>}
    </strong>
  </div>
  <div class="text-slate-900 dark:text-slate-100 leading-relaxed">
    <slot />
  </div>
</div>
