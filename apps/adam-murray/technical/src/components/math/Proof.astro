---
/** Proof — thin wrapper over <Environment kind="proof">. */
import Environment from "./Environment.astro";

export interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<Environment kind="proof" title={title}>
  <slot />
</Environment>
