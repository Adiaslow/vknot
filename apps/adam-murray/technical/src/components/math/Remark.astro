---
/** Remark — thin wrapper over <Environment kind="remark">. */
import Environment from "./Environment.astro";

export interface Props {
  number?: string;
}

const { number } = Astro.props;
---

<Environment kind="remark" number={number}>
  <slot />
</Environment>
