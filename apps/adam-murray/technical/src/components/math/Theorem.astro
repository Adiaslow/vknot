---
/** Theorem — thin wrapper over <Environment kind="theorem">. */
import Environment from "./Environment.astro";

export interface Props {
  title?: string;
  number?: string;
}

const { title, number } = Astro.props;
---

<Environment kind="theorem" title={title} number={number}>
  <slot />
</Environment>
